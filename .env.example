VITE_APP_NAME=NurseAI
# Leave empty until you connect Supabase; app still runs without it while auth is off.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Set to true when Google OAuth + Supabase Auth are configured (see README later).
VITE_ENABLE_AUTH=false
